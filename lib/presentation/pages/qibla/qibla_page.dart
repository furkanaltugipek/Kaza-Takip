import 'package:flutter/material.dart';
import 'package:kaza_takip/presentation/pages/compass_page.dart';

/// Menü uyumluluğu için ince sarmalayıcı — gerçek implementasyon
/// `CompassPage` içindedir.
class QiblaPage extends StatelessWidget {
  const QiblaPage({super.key});

  @override
  Widget build(BuildContext context) => const CompassPage();
}
